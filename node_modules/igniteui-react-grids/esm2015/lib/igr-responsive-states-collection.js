import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { ResponsiveState as ResponsiveState_internal } from './ResponsiveState';
import { IgrResponsiveState as IgrResponsiveState } from './igr-responsive-state';
export class IgrResponsiveStatesCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrResponsiveState.$type) {
            IgrResponsiveState.$type = markType(IgrResponsiveState, "IgrResponsiveState");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrResponsiveState.$type) {
            IgrResponsiveState.$type = markType(IgrResponsiveState, "IgrResponsiveState");
        }
        let coll = new SyncableObservableCollection$2(IgrResponsiveState.$type, ResponsiveState_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = new IgrResponsiveState();
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    }
}
//# sourceMappingURL=igr-responsive-states-collection.js.map
