import { Base, Type } from "igniteui-react-core";
import { ITickProvider } from "igniteui-react-core";
import { DomRenderer } from "igniteui-react-core";
/**
 * @hidden
 */
export declare class BrowserTickProvider extends Base implements ITickProvider {
    static $t: Type;
    private e;
    private c;
    private _requestAnimationFrame;
    private requestAnimationFrame;
    constructor(a: DomRenderer);
    private d;
    private f;
    setupTicking(a: () => void): number;
    private b;
    requestFrame(a: number): void;
    teardownTicking(a: number): void;
}
