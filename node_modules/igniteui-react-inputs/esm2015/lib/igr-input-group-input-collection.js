import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { InputGroupItem as InputGroupItem_internal } from './InputGroupItem';
import { IgrInputGroupItem as IgrInputGroupItem } from './igr-input-group-item';
export class IgrInputGroupInputCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrInputGroupItem.$type) {
            IgrInputGroupItem.$type = markType(IgrInputGroupItem, "IgrInputGroupItem");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrInputGroupItem.$type) {
            IgrInputGroupItem.$type = markType(IgrInputGroupItem, "IgrInputGroupItem");
        }
        let coll = new SyncableObservableCollection$2(IgrInputGroupItem.$type, InputGroupItem_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = IgrInputGroupItem._createFromInternal(int);
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    }
}
//# sourceMappingURL=igr-input-group-input-collection.js.map
