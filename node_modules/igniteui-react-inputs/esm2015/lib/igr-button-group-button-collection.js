import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { Button as Button_internal } from './Button';
import { IgrButton as IgrButton } from './igr-button';
export class IgrButtonGroupButtonCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrButton.$type) {
            IgrButton.$type = markType(IgrButton, "IgrButton");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrButton.$type) {
            IgrButton.$type = markType(IgrButton, "IgrButton");
        }
        let coll = new SyncableObservableCollection$2(IgrButton.$type, Button_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = IgrButton._createFromInternal(int);
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    }
}
//# sourceMappingURL=igr-button-group-button-collection.js.map
